import 'package:flutter/material.dart';

class AppColors {
  // Matches Tailwind Config
  static const Color primary = Color(0xFF000410);    // Dark Blue/Black
  static const Color secondary = Color(0xFFFC9910);  // Secondary Container (Amber)
  static const Color background = Color(0xFFF8F9FF); // Background
  static const Color onSurface = Color(0xFF0B1C30);  // Text primary
  static const Color surfaceVariant = Color(0xFFD3E4FE);
  static const Color secondaryColor = Color(0xFFF8A11C); // Ambre
  static const Color hexagonOverlay = Color(0xFFFFE5B4); // Ambre très clair pour les motifs
  static const Color surface = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF64748B);
  
  // Status Colors
  static const Color success = Color(0xFF10B981);
  static const Color error = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
}
